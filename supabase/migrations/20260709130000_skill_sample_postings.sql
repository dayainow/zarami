-- ==========================================
-- 스킬별 실제 채용 공고 샘플 저장
-- "왜 배워야 할까요" 섹션이 지금까지는 언급 건수(집계)만 보여줬는데,
-- 실제로 지원 가능한 공고 링크(회사명/제목/URL)까지 보여줘야
-- "가이드를 따라가서 원하는 기업에 지원한다"는 목표에 닿을 수 있음.
-- update-trend.ts가 매 실행마다 스킬당 최대 2건을 골라 덮어씀.
-- ==========================================

ALTER TABLE public.skills
  ADD COLUMN IF NOT EXISTS sample_postings JSONB NOT NULL DEFAULT '[]'::jsonb;
