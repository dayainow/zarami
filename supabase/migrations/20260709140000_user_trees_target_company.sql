-- ==========================================
-- 로드맵별 목표 기업/직무 저장
-- "왜 배워야 할까요"까지는 갖췄지만 "어디에 지원할 것인가"가 없었음.
-- 온보딩에서 목표 기업/직무를 입력받아 AI 로드맵 생성 프롬프트에
-- 반영하고, 트리별로 저장해 계속 참고할 수 있게 함.
-- ==========================================

ALTER TABLE public.user_trees
  ADD COLUMN IF NOT EXISTS target_company TEXT;
